import React from 'react';
import Button from '@material-ui/core/Button';
const AddTodo = () => {
  return (
    <div>
      <Button className="add_todo_button" variant="contained" color="secondary">
        Agregar Quehacer
      </Button>
    </div>
  );
};

export default AddTodo;
